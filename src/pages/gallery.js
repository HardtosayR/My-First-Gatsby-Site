import * as React from "react"
import Layout from "../components/layout"

const Gallery = () => {
    return (
        <Layout pageTile="Gallery" pageheading="Gallery">
            <p>辞呈可待成追忆，只是当时已茫然。</p>
        </Layout>
    )
}

export default Gallery